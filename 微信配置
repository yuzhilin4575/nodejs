把写好的 Nodejs 代码部署到服务器上，在 80 端口跑起来，或者通过 nginx 代理到 80 端口，这样可以从外部，
以域名或者 ip 的方式访问到这个 Nodejs 服务，然后把这个 ip 或者域名，配置到公众号后台进行验证这些操作，
之后，就是不断的写代码，不断的把新代码部署到服务器上，然后重新开启服务，来测试，如果想更快一些开发，
就直接在服务器上 vim 开发，改完代码就重启服务
